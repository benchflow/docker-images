minio.client.configure.service: {
  type: oneshot,
  command: "/configure-mc/configure.sh",
  # enabled root access, so that the mc can access to the file system
  uid: root,
  service_groups: INIT,
  restart: true,	
  stdout: inherit,
  stderr: inherit,
}